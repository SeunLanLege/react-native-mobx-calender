# react-native-mobx-calender

To install run
`yarn add react-native-mobx-calender`

Props
====
`events` : `an array of dates to be marked`

`setSelected`: `function called when a date has been selected, with date selected`
